const { expect } = require("chai");

// Tests will be added
describe("BirthdayMoneyCollector contract", function () {
  it("should test anything", async function () {
    expect(1).to.equal(1);
  });
});
